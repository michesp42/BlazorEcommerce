@page "/"
@page "/search/{searchText}/{requestedPage:int}"
@page "/{categoryUrl}"

<PageTitle>My Shop</PageTitle>

@if (SearchText is null && CategoryUrl is null)
{
    <FeaturedProducts />
}
else
{
    <ProductList />
}